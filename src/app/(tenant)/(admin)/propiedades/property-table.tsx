'use client';

import { useMemo, useState, useTransition } from 'react';
import Link from 'next/link';
import { ArrowUpDown, Building, CircleDollarSign, Edit, ExternalLink, KeyRound, Plus, Trash2, UserRound } from 'lucide-react';
import { formatCurrency } from '@/lib/utils';
import { deletePropertyAction, savePropertyAction } from '@/actions/properties';
import { Drawer, FormSection, WorkspaceToolbar } from '@/components/ui/workspace';
import { WorkspaceSearch } from '@/components/ui/workspace-search';

interface PropertyItem { id:string;code:string;address:string;type:string;operation:string;commercialStatus:string;rooms:number|null;bedrooms:number|null;bathrooms:number|null;sqm:number|null;baseRent:number|null;rentPrice:number|null;salePrice:number|null;expenses:number|null;expensesShare:number|null;currency:string;city:string|null;province:string|null;status:string;notes?:string|null;owners?:Array<{id:string;contactId:string;name:string;percentage:number;isPrimary:boolean}>;activeLease?:{id:string;currentRent:number;startDate:Date;endDate:Date;renterName:string;renterPhone?:string|null;renterEmail?:string|null;pendingDebtTotal:number}|null }
const OPERATION_LABELS:Record<string,string>={RENT:'Alquiler',SALE:'Venta',TEMPORARY_RENT:'Alquiler temporal',MANAGEMENT:'Administración'};
const COMMERCIAL_LABELS:Record<string,string>={DRAFT:'Borrador',AVAILABLE:'Disponible',RESERVED:'Reservada',UNDER_NEGOTIATION:'En negociación',CLOSED:'Cerrada',PAUSED:'Pausada',ARCHIVED:'Archivada'};
function money(value:number|null|undefined,currency='ARS'){if(value==null)return null;return `${currency} ${value.toLocaleString('es-AR',{maximumFractionDigits:2})}`}

export function PropertyTable({ initialProperties }: { initialProperties: PropertyItem[] }) {
  const [search,setSearch]=useState(''); const [selectedType,setSelectedType]=useState('ALL'); const [selectedStatus,setSelectedStatus]=useState('ALL'); const [selectedOperation,setSelectedOperation]=useState('ALL'); const [sortAsc,setSortAsc]=useState(true); const [isModalOpen,setIsModalOpen]=useState(false); const [editingProperty,setEditingProperty]=useState<PropertyItem|null>(null); const [error,setError]=useState(''); const [isPending,startTransition]=useTransition();
  const filtered=useMemo(()=>{const q=search.trim().toLowerCase();return initialProperties.filter(property=>{const matchesSearch=!q||[property.code,property.address,property.city,property.province,property.activeLease?.renterName,property.owners?.map(owner=>owner.name).join(' ')].filter(Boolean).some(value=>String(value).toLowerCase().includes(q));return matchesSearch&&(selectedType==='ALL'||property.type===selectedType)&&(selectedStatus==='ALL'||property.status===selectedStatus)&&(selectedOperation==='ALL'||property.operation===selectedOperation)}).sort((a,b)=>{const na=Number(a.code),nb=Number(b.code);if(Number.isFinite(na)&&Number.isFinite(nb))return sortAsc?na-nb:nb-na;return sortAsc?a.code.localeCompare(b.code):b.code.localeCompare(a.code)})},[initialProperties,search,selectedType,selectedStatus,selectedOperation,sortAsc]);
  const summary=useMemo(()=>({rented:initialProperties.filter(p=>p.status==='ALQUILADO').length,available:initialProperties.filter(p=>p.status!=='ALQUILADO'&&p.status!=='MANTENIMIENTO').length,withDebt:initialProperties.filter(p=>(p.activeLease?.pendingDebtTotal||0)>0).length}),[initialProperties]);
  function openModal(property?:PropertyItem){setEditingProperty(property||null);setError('');setIsModalOpen(true)} function closeModal(){setEditingProperty(null);setError('');setIsModalOpen(false)}
  function handleSubmit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const f=new FormData(event.currentTarget);const operation=String(f.get('operation')||'RENT');const rentPrice=f.get('rentPrice')?Number(f.get('rentPrice')):null;startTransition(async()=>{try{setError('');await savePropertyAction({id:editingProperty?.id,code:String(f.get('code')||''),address:String(f.get('address')||''),type:String(f.get('type')||'DEPARTAMENTO') as any,operation:operation as any,commercialStatus:String(f.get('commercialStatus')||'AVAILABLE') as any,rooms:f.get('rooms')?Number(f.get('rooms')):null,bedrooms:f.get('bedrooms')?Number(f.get('bedrooms')):null,bathrooms:f.get('bathrooms')?Number(f.get('bathrooms')):null,sqm:f.get('sqm')?Number(f.get('sqm')):null,baseRent:['RENT','TEMPORARY_RENT','MANAGEMENT'].includes(operation)?rentPrice:null,rentPrice,salePrice:f.get('salePrice')?Number(f.get('salePrice')):null,expenses:f.get('expenses')?Number(f.get('expenses')):null,expensesShare:f.get('expensesShare')?Number(f.get('expensesShare')):null,currency:String(f.get('currency')||'ARS'),city:String(f.get('city')||'')||null,province:String(f.get('province')||'')||null,notes:String(f.get('notes')||'')||undefined});closeModal();window.location.reload()}catch(err:any){setError(err?.message||'No se pudo guardar la propiedad.')}})}
  function handleDelete(id:string){if(!confirm('¿Seguro que deseás archivar esta propiedad?'))return;startTransition(async()=>{try{await deletePropertyAction(id);window.location.reload()}catch(err:any){setError(err?.message||'No se pudo archivar la propiedad.')}})}
  const summaryCards=[{label:'Total',value:initialProperties.length,icon:Building,tone:'text-indigo-600 bg-indigo-50'},{label:'Alquiladas',value:summary.rented,icon:KeyRound,tone:'text-emerald-700 bg-emerald-50'},{label:'Disponibles',value:summary.available,icon:Building,tone:'text-sky-700 bg-sky-50'},{label:'Con saldo',value:summary.withDebt,icon:CircleDollarSign,tone:summary.withDebt?'text-rose-700 bg-rose-50':'text-slate-600 bg-slate-100'}];
  return <div className="space-y-4">
    <section className="grid grid-cols-2 xl:grid-cols-4 gap-3">{summaryCards.map(item=>{const Icon=item.icon;return <div key={item.label} className="flex items-center gap-3 rounded-xl border border-slate-200 bg-white px-4 py-3 shadow-xs"><div className={`w-9 h-9 rounded-lg flex items-center justify-center ${item.tone}`}><Icon className="w-4 h-4"/></div><div><p className="text-[9px] uppercase tracking-[.1em] font-bold text-slate-400">{item.label}</p><p className="text-lg leading-none font-extrabold text-slate-900 mt-1">{item.value}</p></div></div>})}</section>
    <WorkspaceToolbar><div className="ui-filter-row"><WorkspaceSearch value={search} onChange={setSearch} placeholder="Buscar código, dirección, localidad, inquilino o propietario..."/><FilterSelect value={selectedOperation} onChange={setSelectedOperation} options={[["ALL","Todas las operaciones"],["RENT","Alquiler"],["SALE","Venta"],["TEMPORARY_RENT","Temporal"],["MANAGEMENT","Administración"]]}/><FilterSelect value={selectedType} onChange={setSelectedType} options={[["ALL","Todos los tipos"],["DEPARTAMENTO","Departamento"],["CASA","Casa"],["LOCAL","Local"],["TERRENO","Terreno"],["OFICINA","Oficina"],["COCHERA","Cochera"],["OTRO","Otro"]]}/><FilterSelect value={selectedStatus} onChange={setSelectedStatus} options={[["ALL","Todos los estados"],["DISPONIBLE","Disponible"],["ALQUILADO","Alquilado"],["MANTENIMIENTO","Mantenimiento"]]}/><button type="button" onClick={()=>setSortAsc(v=>!v)} className="ui-action-secondary"><ArrowUpDown className="w-3.5 h-3.5"/>Código {sortAsc?'↑':'↓'}</button></div><button type="button" onClick={()=>openModal()} className="btn-primary"><Plus className="w-4 h-4"/>Nueva propiedad</button></WorkspaceToolbar>
    {error&&!isModalOpen&&<div className="p-3 bg-rose-50 border border-rose-100 text-rose-700 text-sm rounded-xl">{error}</div>}
    <div className="flex items-center justify-between px-1"><p className="text-[10px] text-slate-500"><strong className="text-slate-800">{filtered.length}</strong> propiedades en esta vista</p><p className="text-[9px] uppercase tracking-[.12em] font-bold text-slate-400">Ficha 360 disponible</p></div>
    <section className="property-grid">{filtered.map(property=>{const hasDebt=(property.activeLease?.pendingDebtTotal||0)>0;const mainPrice=property.operation==='SALE'?property.salePrice:property.rentPrice??property.baseRent;const ownerName=property.owners?.find(owner=>owner.isPrimary)?.name||property.owners?.[0]?.name;const statusClass=property.status==='ALQUILADO'?'property-status--rented':property.status==='MANTENIMIENTO'?'property-status--maintenance':'property-status--available';const statusLabel=property.status==='ALQUILADO'?'Alquilada':property.status==='MANTENIMIENTO'?'Mantenimiento':COMMERCIAL_LABELS[property.commercialStatus]||'Disponible';return <article key={property.id} className="property-card"><div className="property-card__body"><div className="property-card__top"><div><div className="property-code"><span>{property.code}</span><span className="!bg-transparent !p-0 !min-w-0 !font-sans !h-auto">Inmueble</span></div><p className="property-operation">{OPERATION_LABELS[property.operation]||property.operation}</p></div><span className={`property-status ${statusClass}`}>{statusLabel}</span></div><p className="property-type">{property.type}</p><h3 className="property-address">{property.address}</h3>{(property.city||property.province)&&<p className="property-location">{[property.city,property.province].filter(Boolean).join(' · ')}</p>}{(property.rooms!=null||property.bedrooms!=null||property.bathrooms!=null||property.sqm!=null)&&<div className="property-features">{property.rooms!=null&&<span className="property-feature">{property.rooms} amb.</span>}{property.bedrooms!=null&&<span className="property-feature">{property.bedrooms} dorm.</span>}{property.bathrooms!=null&&<span className="property-feature">{property.bathrooms} baño/s</span>}{property.sqm!=null&&<span className="property-feature">{property.sqm} m²</span>}</div>}{mainPrice!=null&&<div className="property-finance"><div><small>{property.operation==='SALE'?'Precio de venta':'Valor mensual'}</small><strong>{money(mainPrice,property.currency)}</strong></div>{property.expenses!=null&&<div className="text-right"><small>Expensas</small><strong className="!text-[10px] !font-semibold">{money(property.expenses,property.currency)}</strong></div>}</div>}{ownerName&&<div className="property-person"><div className="flex items-center gap-2"><UserRound className="w-3.5 h-3.5 text-indigo-500"/><span className="property-person__label">Propietario</span></div><p className="property-person__name">{ownerName}</p></div>}{property.activeLease&&<div className="property-person"><div className="flex items-center gap-2"><KeyRound className="w-3.5 h-3.5 text-emerald-600"/><span className="property-person__label">Ocupación actual</span></div><p className="property-person__name">{property.activeLease.renterName}</p><div className="property-person__meta"><span>{formatCurrency(property.activeLease.currentRent)}/mes</span>{hasDebt&&<span className="property-person__debt">Saldo {formatCurrency(property.activeLease.pendingDebtTotal)}</span>}</div></div>}</div><footer className="property-card__footer"><Link href={`/propiedades/${property.id}`} className="property-card__link">Abrir propiedad 360 <ExternalLink className="w-3.5 h-3.5"/></Link><div className="flex items-center gap-1.5"><button type="button" onClick={()=>openModal(property)} className="icon-action" title="Editar propiedad"><Edit className="w-3.5 h-3.5"/></button><button type="button" onClick={()=>handleDelete(property.id)} className="icon-action hover:!text-rose-600" title="Archivar"><Trash2 className="w-3.5 h-3.5"/></button></div></footer></article>})}</section>
    <Drawer open={isModalOpen} onClose={closeModal} title={editingProperty?`Editar ${editingProperty.code}`:'Nueva propiedad'} subtitle="La carga está separada en bloques para mantener foco y reducir scroll cognitivo." width="wide"><form onSubmit={handleSubmit}>{error&&<div className="mb-4 p-3 bg-rose-50 border border-rose-100 text-rose-700 text-sm rounded-lg">{error}</div>}<FormSection title="Identificación comercial"><Field name="code" label="Código interno *" required defaultValue={editingProperty?.code||''}/><SelectField name="type" label="Tipo *" defaultValue={editingProperty?.type||'DEPARTAMENTO'} options={[["DEPARTAMENTO","Departamento"],["CASA","Casa"],["LOCAL","Local"],["TERRENO","Terreno"],["OFICINA","Oficina"],["COCHERA","Cochera"],["OTRO","Otro"]]}/><SelectField name="operation" label="Operación *" defaultValue={editingProperty?.operation||'RENT'} options={[["RENT","Alquiler"],["SALE","Venta"],["TEMPORARY_RENT","Alquiler temporal"],["MANAGEMENT","Administración"]]}/><SelectField name="commercialStatus" label="Estado comercial" defaultValue={editingProperty?.commercialStatus||'AVAILABLE'} options={[["DRAFT","Borrador"],["AVAILABLE","Disponible"],["RESERVED","Reservada"],["UNDER_NEGOTIATION","En negociación"],["CLOSED","Cerrada"],["PAUSED","Pausada"]]}/></FormSection><FormSection title="Ubicación"><div className="col-span-full"><Field name="address" label="Dirección completa *" required defaultValue={editingProperty?.address||''}/></div><Field name="city" label="Localidad" defaultValue={editingProperty?.city||''}/><Field name="province" label="Provincia" defaultValue={editingProperty?.province||''}/></FormSection><FormSection title="Características"><Field name="rooms" label="Ambientes" type="number" min="0" defaultValue={editingProperty?.rooms??''}/><Field name="bedrooms" label="Dormitorios" type="number" min="0" defaultValue={editingProperty?.bedrooms??''}/><Field name="bathrooms" label="Baños" type="number" min="0" defaultValue={editingProperty?.bathrooms??''}/><Field name="sqm" label="Superficie total m²" type="number" min="0" step="0.01" defaultValue={editingProperty?.sqm??''}/></FormSection><FormSection title="Valores"><SelectField name="currency" label="Moneda" defaultValue={editingProperty?.currency||'ARS'} options={[["ARS","ARS"],["USD","USD"],["EUR","EUR"]]}/><Field name="rentPrice" label="Precio alquiler" type="number" min="0" step="0.01" defaultValue={editingProperty?.rentPrice??editingProperty?.baseRent??''}/><Field name="salePrice" label="Precio venta" type="number" min="0" step="0.01" defaultValue={editingProperty?.salePrice??''}/><Field name="expenses" label="Expensas" type="number" min="0" step="0.01" defaultValue={editingProperty?.expenses??''}/><Field name="expensesShare" label="% expensas a cargo" type="number" min="0" max="100" step="0.01" defaultValue={editingProperty?.expensesShare??''}/><label><span className="form-label">Notas internas</span><textarea name="notes" rows={3} defaultValue={editingProperty?.notes||''} className="form-input"/></label></FormSection><div className="ui-form-actions"><button type="button" onClick={closeModal} className="btn-secondary">Cancelar</button><button type="submit" disabled={isPending} className="btn-primary">{isPending?'Guardando...':'Guardar propiedad'}</button></div></form></Drawer>
  </div>
}
function FilterSelect({value,onChange,options}:{value:string;onChange:(value:string)=>void;options:Array<[string,string]>}){return <select value={value} onChange={e=>onChange(e.target.value)} className="form-input !w-auto">{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select>}
function SelectField({name,label,defaultValue,options}:{name:string;label:string;defaultValue:string;options:Array<[string,string]>}){return <label><span className="form-label">{label}</span><select name={name} defaultValue={defaultValue} className="form-input">{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>}
function Field({name,label,defaultValue,type='text',required,min,max,step}:{name:string;label:string;defaultValue:string|number;type?:string;required?:boolean;min?:string;max?:string;step?:string}){return <label><span className="form-label">{label}</span><input name={name} type={type} required={required} min={min} max={max} step={step} defaultValue={defaultValue} className="form-input"/></label>}
